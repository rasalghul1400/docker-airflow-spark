1. sudo chmod 777 -R ./dags ./doc ./docker ./logs ./notebooks ./spark
2. minio .yaml 
3. https://github.com/njanakiev/trino-minio-docker/blob/master/docker-compose.yml inspo 
4. mod .yaml 
5. connecting to a hive metatore requires trino?
6. S3 command  [https://s3tools.org/s3cmd]

< line sudo apt update
sudo apt install -y \
    s3cmd \
    openjdk-11-jre-headless  # Needed for trino-cli >

7. add conf file 
7.1 rename the test init for postgres to metastore
8. inside the conf file add xml 
9. chnage the minio to newer version

. docker compose  -f docker-compose.yaml  -f docker-compose.spark.yaml up -d 
. docker compose  -f docker-compose.yaml  -f docker-compose.spark.yaml down --remove-orphans -v


s3cmd --config minio.s3cfg --configure

Access Key: minio_access_key
Secret Key: minio_secret_key
Default Region [US]:
S3 Endpoint [s3.amazonaws.com]: localhost:9000
DNS-style bucket+hostname:port template for accessing a bucket [%(bucket)s.s3.amazonaws.com]: localhost:9000
Encryption password:
Path to GPG program [/usr/bin/gpg]:
Use HTTPS protocol [Yes]: no

s3cmd --config minio.s3cfg put /workspace/docker-airflow-spark/minio-data/iris.parq s3://iris



wget https://repo1.maven.org/maven2/io/trino/trino-cli/390/trino-cli-390-executable.jar \
  -O trino
chmod +x trino

./trino --execute "
SHOW CATALOGS;"


git add path/to/folder/.gitignore

git add logs/.gitignore

git add path/to/folder/.gitignore